<h1><%= ride.escape('name') %></h1>
<br>
<img src="<%= ride.escape('image_url') %>" alt="<%= ride.escape('name') %>">
<br>
<br>
<p><%= ride.escape('description') %></p>
<div class="reviews-window">
  <div id="reviews">
  </div>

  <div id="review-form">
  </div>
</div>
